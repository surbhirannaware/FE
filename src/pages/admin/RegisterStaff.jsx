 import React, { useState } from "react"; import toast from "react-hot-toast"; import api from "../../api"; function RegisterStaff() { const [form, setForm] = useState({ fullName: "", phoneNo: "", password: "", email: "", }); const [loading, setLoading] = useState(false); const handleChange = (e) => { setForm({ ...form, [e.target.name]: e.target.value, }); }; const validate = () => { const phoneRegex = /^[6-9]\d{9}$/; if (!form.fullName.trim()) { toast.error("Full name required"); return false; } if (!phoneRegex.test(form.phoneNo)) { toast.error("Enter valid Indian mobile number"); return false; } if (form.password.length < 6) { toast.error("Password must be 6+ characters"); return false; } return true; }; const handleSubmit = async (e) => { e.preventDefault(); if (!validate()) return; try { setLoading(true); const token = localStorage.getItem("token"); await api.post( "/api/auth/register", { ...form, roleId: 5, }, { headers: { Authorization: `Bearer ${token}`, }, } ); toast.success("Staff registered successfully"); setForm({ fullName: "", phoneNo: "", password: "", email: "", }); } catch (err) { console.error(err); toast.error( err.response?.data?.message || err.response?.data || "Something went wrong" ); } finally { setLoading(false); } }; return ( <div className="max-w-md mx-auto bg-white p-8 rounded-xl shadow"> <h2 className="text-2xl font-bold mb-6">Register Staff</h2> <form onSubmit={handleSubmit} className="space-y-4"> <input name="fullName" placeholder="Full Name" value={form.fullName} onChange={handleChange} className="w-full p-3 border rounded" /> <input name="phoneNo" placeholder="Phone Number" value={form.phoneNo} onChange={handleChange} className="w-full p-3 border rounded" /> <input name="email" placeholder="Email (Optional)" value={form.email} onChange={handleChange} className="w-full p-3 border rounded" /> <input name="password" type="password" placeholder="Password" value={form.password} onChange={handleChange} className="w-full p-3 border rounded" /> <button type="submit" disabled={loading} className="w-full bg-blue-600 text-white p-3 rounded" > {loading ? "Registering..." : "Create Staff"} </button> </form> </div> ); } export default RegisterStaff;