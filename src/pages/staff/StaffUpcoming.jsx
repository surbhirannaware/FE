 import { useEffect, useState } from "react"; import { getUpcomingAppointments } from "../../services/staffApi"; import toast from "react-hot-toast"; import React from "react"; function StaffUpcoming() { const [appointments, setAppointments] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const fetchData = async () => { try { setLoading(true); const data = await getUpcomingAppointments(); setAppointments(Array.isArray(data) ? data : []); } catch (err) { console.error(err); toast.error("Failed to load upcoming appointments"); setAppointments([]); } finally { setLoading(false); } }; fetchData(); }, []); const formatDate = (dateValue) => { if (!dateValue) return "-"; return new Date(dateValue).toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric", }); }; const formatTime = (time) => { if (!time) return ""; return new Date(`1970-01-01T${time}`).toLocaleTimeString("en-IN", { hour: "2-digit", minute: "2-digit", hour12: true, }); }; if (loading) { return <div className="p-6 text-gray-500">Loading upcoming appointments...</div>; } return ( <div className="p-6"> <h2 className="text-2xl font-bold mb-4">Upcoming Appointments</h2> {appointments.length === 0 ? ( <p className="text-gray-500">No upcoming appointments</p> ) : ( appointments.map((appt) => ( <div key={appt.appointmentId} className="bg-white shadow rounded p-4 mb-3" > <p className="font-semibold">{appt.customerName}</p> <p className="text-sm text-gray-500"> {formatDate(appt.appointmentDate)} | {formatTime(appt.startTime)} -{" "} {formatTime(appt.endTime)} </p> <div className="mt-2"> {appt.services?.map((s, i) => ( <span key={i} className="bg-blue-100 text-sm px-2 py-1 rounded mr-2" > {s} </span> ))} {appt.isOngoing && ( <span className="text-green-600 font-bold ml-2">Ongoing</span> )} {appt.isUpcoming && ( <span className="text-blue-600 font-bold ml-2">Upcoming</span> )} </div> </div> )) )} </div> ); } export default StaffUpcoming;