 import { useEffect, useState } from "react"; import { getTodayAppointments, completeAppointment, } from "../../services/staffApi"; import toast from "react-hot-toast"; import React from "react"; function StaffToday() { const [data, setData] = useState(null); const [loading, setLoading] = useState(true); const [completingId, setCompletingId] = useState(null); const fetchData = async () => { try { setLoading(true); const result = await getTodayAppointments(); setData(result || {}); } catch (err) { console.error("API ERROR:", err); toast.error("Failed to load appointments"); setData(null); } finally { setLoading(false); } }; useEffect(() => { fetchData(); }, []); const handleComplete = async (id) => { try { setCompletingId(id); await completeAppointment(id); toast.success("Appointment completed"); fetchData(); } catch (err) { console.error(err); toast.error("Error completing appointment"); } finally { setCompletingId(null); } }; const formatTime = (time) => { if (!time) return ""; return new Date(`1970-01-01T${time}`).toLocaleTimeString("en-IN", { hour: "2-digit", minute: "2-digit", hour12: true, }); }; if (loading) return <p>Loading...</p>; return ( <div className="p-6 space-y-6"> <div className="grid grid-cols-1 md:grid-cols-4 gap-4"> <div className="bg-blue-500 text-white p-4 rounded-xl shadow"> <p className="text-sm">Total Appointments</p> <h3 className="text-2xl font-bold">{data?.totalAppointments ?? 0}</h3> </div> <div className="bg-green-500 text-white p-4 rounded-xl shadow"> <p className="text-sm">Completed</p> <h3 className="text-2xl font-bold"> {data?.completedAppointments ?? 0} </h3> </div> <div className="bg-yellow-500 text-white p-4 rounded-xl shadow"> <p className="text-sm">Pending</p> <h3 className="text-2xl font-bold"> {data?.pendingAppointments ?? 0} </h3> </div> <div className="bg-purple-500 text-white p-4 rounded-xl shadow"> <p className="text-sm">Today's Earnings</p> <h3 className="text-2xl font-bold">₹ {data?.todayEarnings ?? 0}</h3> </div> </div> <div> <h2 className="text-2xl font-bold mb-4">Today's Appointments</h2> {data?.appointments?.length === 0 && ( <p className="text-gray-500">No appointments today</p> )} {data?.appointments?.map((appt) => ( <div key={appt.appointmentId} className="bg-white shadow-md rounded-xl p-4 mb-4" > <div className="flex justify-between items-center"> <div> <p className="font-semibold text-lg">{appt.customerName}</p> <p className="text-sm text-gray-500"> {formatTime(appt.startTime)} - {formatTime(appt.endTime)} </p> <div className="mt-2"> {appt.services?.map((s, i) => ( <span key={i} className="bg-gray-200 text-sm px-2 py-1 rounded mr-2" > {s} </span> ))} </div> </div> {appt.status === "Booked" && ( <button onClick={() => handleComplete(appt.appointmentId)} disabled={completingId === appt.appointmentId} className="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 disabled:opacity-60" > {completingId === appt.appointmentId ? "Completing..." : "Complete"} </button> )} {appt.status === "Completed" && ( <span className="text-green-600 font-semibold"> Completed </span> )} </div> </div> ))} </div> </div> ); } export default StaffToday;